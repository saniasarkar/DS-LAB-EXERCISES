#include <stdio.h>

int main() {
    int #include <stdio.h>

int queue[20], front = -1, rear = -1;
int visited[20], graph[20][20], n;

void bfs(int start) {
    int i;
    queue[++rear] = start;
    visited[start] = 1;

    while(front != rear) {
        start = queue[++front];
        printf("%d ", start);

        for(i = 0; i < n; i++) {
            if(graph[start][i] == 1 && visited[i] == 0) {
                queue[++rear] = i;
                visited[i] = 1;
            }
        }
    }
}

int main() {
    int i, j, start;

    printf("Enter number of vertices: ");
    scanf("%d", &n);

    printf("Enter adjacency matrix:\n");
    for(i = 0; i < n; i++)
        for(j = 0; j < n; j++)
            scanf("%d", &graph[i][j]);

    printf("Enter starting vertex: ");
    scanf("%d", &start);

    bfs(start);
    return 0;
}[10][10], dist[10], visited[10];
    int n, i, j, source, min, u;

    printf("Enter number of vertices: ");
    scanf("%d", &n);

    printf("Enter cost matrix:\n");
    for(i = 0; i < n; i++)
        for(j = 0; j < n; j++)
            scanf("%d", &cost[i][j]);

    printf("Enter source vertex: ");
    scanf("%d", &source);

    for(i = 0; i < n; i++) {
        dist[i] = cost[source][i];
        visited[i] = 0;
    }

    visited[source] = 1;

    for(i = 1; i < n; i++) {
        min = 999;
        for(j = 0; j < n; j++) {
            if(!visited[j] && dist[j] < min) {
                min = dist[j];
                u = j;
            }
        }

        visited[u] = 1;

        for(j = 0; j < n; j++) {
            if(!visited[j] && dist[j] > dist[u] + cost[u][j])
                dist[j] = dist[u] + cost[u][j];
        }
    }

    printf("Shortest distances from source:\n");
    for(i = 0; i < n; i++)
        printf("%d -> %d = %d\n", source, i, dist[i]);

    return 0;
}
